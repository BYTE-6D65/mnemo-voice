[project]
name = "mnemo-voice"
version = "0.1.0"
description = "WebSocket voice channel adapter for Hermes agent"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]
